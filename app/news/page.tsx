import type { NextComponentType, NextPageContext } from 'next';

// TODO: News page
// News page includes three latest news
// Idea is to have grid with photo in first half and short description in a second

interface Props {}

const Page: NextComponentType<NextPageContext, {}, Props> = (
  props: Props,
) => {
  return <div></div>;
};

export default Page;
