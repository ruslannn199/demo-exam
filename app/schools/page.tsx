import type { NextComponentType, NextPageContext } from 'next';

// TODO: Make schools page
// This page includes photos, description and contact info
// There's schools in five cities: Tomsk, Kemerovo, Novosibirsk, Barnaul and Krasnoyarsk
// Idea is to have carousel with different schools and their info on a map?

interface Props {}

const Page: NextComponentType<NextPageContext, {}, Props> = (
  props: Props,
) => {
  return <div></div>;
};

export default Page;
